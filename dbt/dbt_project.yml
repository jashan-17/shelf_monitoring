name: 'shelf_monitoring'
version: '1.0'
config-version: 2

profile: 'shelf_monitoring'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  shelf_monitoring:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table